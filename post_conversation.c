#include <stdio.h>
#include <stdlib.h>

char *post_conversion(char *S);

int main()
{
    char S[64] = "2 + 2";

    return 0;
}


char *post_conversion (char* S)
{   
    assert(S == NULL);

    char *new_string = (char*) calloc(256, sizeof(char));
    char elem = 'A';

    while (elem != '\0')
    {

    }
}